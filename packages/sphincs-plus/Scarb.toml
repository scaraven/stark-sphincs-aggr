[package]
name = "sphincs_plus"
version = "0.1.0"
edition = "2024_07"

[lib]

[executable]

[dependencies]
cairo_execute = "2.11.4"
oracle = "0.1.0-dev.4"

[dev-dependencies]
cairo_test = "2.11.4"

[features]
default = []
# Hash backend selection (enable at most one of these in a build):
blake_hash = []
poseidon_hash = []
sparse_addr = []